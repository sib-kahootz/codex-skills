# pull-request

Prepares, reviews, updates, pushes, and creates GitHub pull requests from local branch work, defaulting to draft PRs unless requested otherwise.

## Labels

The skill derives likely label terms from the pull request's net diff and searches the target repository only for those candidates. It does not require local label configuration, enumerate all labels, or invent labels that are absent from the repository.
