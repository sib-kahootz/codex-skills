# new-branch

Creates and switches to a Git branch named from a Jira issue key and summary. It updates the selected base branch (defaulting to `master`), validates the generated branch name, and avoids collisions with existing local or remote branches.
