# openspec-evolve

Keeps OpenSpec artifacts aligned with requirements discovered after `/opsx:apply`. It records the requirement delta, verifies the updated specification, and identifies whether no implementation work is needed or a further `/opsx:apply` should run.
